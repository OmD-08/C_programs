#include<stdio.h>
int main(){
	int arr[] = {50,60,90,80,95,45,56}; // Definie an array at single statement
    int i;
	
  // using the for  loop to perform  or accessing it's value through indexing :-

	for(i=0;i<arr[i];i++){
		printf("arr[%d] = %d\n",i,arr[i]);
	}	

	return 0;
}
